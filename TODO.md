# TODO

- Add an optional "straight mouse paths" mode that replaces dense recorded move samples with a direct path from point A to point B before click targets.
- Add presets or saved macros so a user can keep more than one recording.
- Add a safety delay before playback starts, giving the user a moment to focus the target window.
